# M4-T007 治理数据与审计交付记录

状态：完成

- 扩展既有 `/admin-api/community/dashboard`，沿用 `community:dashboard:view` 权限，不新增静态统计接口。
- 看板显示待处理举报与申诉、有效处罚、近七日反滥用拒绝数量、近七日举报/处罚/风险拒绝趋势，以及已结案举报的平均处置分钟数。
- 最近审计事件统一汇总举报、申诉、处罚事件与被拒绝的反滥用事件，按发生时间倒序展示；不暴露举报证据、用户正文或令牌。
- 管理端运营总览提供治理趋势图、待办跳转和审计事件表，加载失败与空数据状态沿用既有页面处理。

验证：管理 API 定向单元测试、管理端 typecheck、lint、test 与 build 已通过。
