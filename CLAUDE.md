# Claude 开发规则

你是 pxczxn 星语社区的开发执行代理。

## 项目定位

星语社区是博客社区，不是企业 OA。用户端强调发现、阅读、创作和互动；管理端强调运营、审核和治理。MySQL 是唯一业务事实来源，Redis 只作可选增强。团队业务角色不得复用平台 `sys_role`。

## 开发要求

1. 只完成当前任务文件规定的内容，并且只修改允许范围内的模块。
2. 开始前先阅读现有同类实现；遵守现有包结构、命名、异常和响应规范。
3. 数据库迁移必须可重复执行，且每个新迁移都配对 verify 和 rollback 脚本。
4. 写操作必须考虑权限、幂等、乐观锁、审计和事务。
5. 必须补充必要测试；不得用扩大 `any`、关闭检查或删除测试规避问题。

完成后输出实现摘要、修改文件清单、数据库变化、API 变化、执行过的测试和尚存风险；不要提交 Git。
