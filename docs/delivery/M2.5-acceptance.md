# M2.5 产品语义与信息架构校正验收记录

> 完成日期：2026-07-27  
> 范围：博客端入口与发现、正式路由、管理端运营信息架构、运行时示例数据与回归验证。

## 已交付结果

- 根路径、无回跳登录和未登录浏览入口统一进入 `/discover`；登录只接受站内相对 `returnTo`，防止开放重定向。
- `/discover` 使用真实全站公开文章、公开动态、平台标签和关注博客文章：
  - 推荐明确表示为近期公开内容；
  - 最新按公开时间；
  - 热门按点赞、收藏、评论和转发等互动热度；
  - 关注展示已关注博客的最新公开文章。
- 博客端顶栏统一提供：首页、发现、文章、动态、系列、团队、标签，以及写文章、通知、聊天、我的博客、收藏夹和设置。
- 固定原型页面已替换为参数化路由：`/moments/{momentId}`、`/teams/{teamSlug}`、`/collaboration/articles/{articleId}`。尚未进入 M3 的团队、投稿、系列和共创功能只显示明确规划提示。
- 管理端默认打开“运营总览”；一级入口收敛为社区运营菜单和“系统设置”。保留的监控、审计、文件与开发工具均下沉到系统设置，不干扰社区业务。
- `V020`—`V022` 迁移完成菜单树重组、平台人员与权限分组、运行时示例组织/岗位文案改名，以及 Mars/测试活动账户停用。

## 运行时菜单结构

```text
运营总览
用户与博客
内容管理
审核中心
社区治理
团队管理（M3 规划页）
标签与系列（系列为 M3 规划页）
通知管理
即时聊天
数据分析（M5 规划页）
系统设置
└─ 平台人员与权限
```

## 验证结果

| 门禁 | 结果 |
| --- | --- |
| 后端 Reactor 测试 | 通过；新增发现流控制器契约测试后，`PublicArticleControllerTest` 4/4 通过 |
| 博客端 `typecheck` / `lint` / `test` / `build` | 通过；渲染与路由回归 5/5 通过 |
| 管理端 `typecheck` / `lint` / `test` / `build` | 通过；测试 6/6 通过。Lint 有 72 条既存 warning、无 error，M2.5 未新增 warning |
| 数据库迁移 | `V001`—`V022` 迁移历史、checksum 与 22 份验证 SQL 全部通过 |
| 真实后端 E2E | 聊天、评论、动态、通知、后台治理全部通过，覆盖持久化、WebSocket 一次性 Ticket、审核状态机、RBAC 与审计事件 |

## 明确留待后续阶段

- M3：团队申请/成员/权限、工作台、投稿、系列、联合创作的真实业务与数据模型。
- M5：运营可配置的编辑精选、跨对象关注流、统一搜索、RSS、SEO、创作者统计与性能优化。
- M6：生产部署、CI/CD、备份恢复、监控告警和公网灰度。
