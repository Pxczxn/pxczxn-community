import type { MetadataRoute } from "next"; import { publicApi,siteUrl } from "./lib/seo";
type ArticlePage={records:Array<{canonicalPath:string;updatedAt:string}>};type Series={id:string;updatedAt:string};
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=siteUrl();const [articles,series]=await Promise.all([publicApi<ArticlePage>("/api/v1/public/articles?pageNum=1&pageSize=50"),publicApi<Series[]>("/api/v1/series")]);return[{url:base,lastModified:new Date(),changeFrequency:"daily",priority:1},{url:`${base}/articles`,changeFrequency:"daily",priority:.8},{url:`${base}/discover`,changeFrequency:"daily",priority:.8},{url:`${base}/editorial`,changeFrequency:"weekly",priority:.7},...(articles?.records||[]).map(i=>({url:`${base}${i.canonicalPath}`,lastModified:new Date(i.updatedAt),changeFrequency:"weekly" as const,priority:.7})),...(series||[]).map(i=>({url:`${base}/series/${i.id}`,lastModified:new Date(i.updatedAt),changeFrequency:"weekly" as const,priority:.6}))];}
